using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileState { 
    
    UNAVAILABLE = -1,
    UNCLAIMED = 0,
    CLAIMED = 1,
    IN_DEVELOPMENT = 1,
    TERRAFORMED = 5

}
